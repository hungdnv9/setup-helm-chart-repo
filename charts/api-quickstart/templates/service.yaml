apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-quickstart.fullname" . }}
  labels:
    {{- include "api-quickstart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-quickstart.selectorLabels" . | nindent 4 }}
